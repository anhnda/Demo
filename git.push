#!/bin/bash

# Assign $1 to MESSAGE, or "Update" if $1 is empty
MESSAGE=${1:-Update}

git add .

# We use "$MESSAGE" in quotes to handle spaces correctly
git commit -m "$MESSAGE"

git push origin dev2